{
  "name": "schorle",
  "version": "0.0.1",
  "main": "main.ts",
  "repository": "https://github.com/renardeinside/schorle",
  "license": "MIT",
  "scripts": {
    "build": "esbuild main.ts --bundle --minify --outfile=../python/schorle/assets/bundle.js",
    "watch": "esbuild main.ts --bundle --minify --outfile=../python/schorle/assets/bundle.js --watch",
    "format": "prettier . --write"
  },
  "dependencies": {
    "lucide": "^0.335.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.1",
    "prettier": "3.2.5"
  }
}
